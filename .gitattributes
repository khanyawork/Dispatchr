# Normalize line endings to LF in the repository regardless of each
# contributor's local core.autocrlf setting, so Flutter-regenerated files
# (generated_plugin_registrant.*, etc.) stop showing as spuriously
# modified across Windows/macOS/Linux machines.
* text=auto eol=lf
